<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="KpiModelLineMapper">
	
	<sql id="kpiModelLineColumns">
		ID,
		MODEL_ID,	
		KPI_CODE,	
		KPI_NAME,
		KPI_TYPE,
		KPI_UNIT,
		STATUS,
		ENABLE,
		CREATE_USER,	
		CREATE_TIME,	
		LAST_UPDATE_USER,	
		LAST_UPDATE_TIME,
		PREPARATION1,
		PREPARATION2,
		PREPARATION3,
		PREPARATION4,
		PREPARATION5,
		PREPARATION6
	</sql>
	
	<!-- 更改分值 -->
	<update id="updateScore" parameterType="pd">
		update  BD_KPI_MODEL_LINE
			set 
				LAST_UPDATE_USER = #{LAST_UPDATE_USER},
				LAST_UPDATE_TIME = #{LAST_UPDATE_TIME},
				PREPARATION1 = #{PREPARATION1},
				PREPARATION2 = #{PREPARATION2},
				PREPARATION3 = #{PREPARATION3},
				PREPARATION4 = #{PREPARATION4}
			where 
				ID = #{ID}
	</update>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into BD_KPI_MODEL_LINE(
			MODEL_ID,	
			KPI_CODE,	
			KPI_NAME,
			KPI_TYPE,
			KPI_UNIT,
			STATUS,
			ENABLE,
			CREATE_USER,	
			CREATE_TIME,	
			LAST_UPDATE_USER,	
			LAST_UPDATE_TIME,
			PREPARATION1,
			PREPARATION2,
			PREPARATION3,
			PREPARATION4,
			PREPARATION5,
			PREPARATION6
		) values (
			#{MODEL_ID},	
			#{KPI_CODE},	
			#{KPI_NAME},	
			#{KPI_TYPE},	
			#{KPI_UNIT},
			0,
			#{ENABLE},
			#{CREATE_USER},	
			#{CREATE_TIME},	
			#{LAST_UPDATE_USER},	
			#{LAST_UPDATE_TIME},
			#{PREPARATION1},
			#{KPI_DESCRIPTION},
			#{PREPARATION3},
			#{PREPARATION4},
			#{PREPARATION5},
			#{PREPARATION6}
			
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from BD_KPI_MODEL_LINE
		where 
			ID = #{ID}
	</delete>
	
	<!-- 根据MODEL_ID删除-->
	<delete id="deleteByModelId" parameterType="pd">
		delete from BD_KPI_MODEL_LINE
		where 
			MODEL_ID = #{MODEL_ID}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update BD_KPI_MODEL_LINE
			set 
				<!-- KPI_CODE = #{KPI_CODE}, -->
				KPI_NAME = #{KPI_NAME},
				KPI_TYPE = #{KPI_TYPE},
				KPI_UNIT = #{KPI_UNIT},
				ENABLE = #{ENABLE},
				LAST_UPDATE_USER = #{LAST_UPDATE_USER},
				LAST_UPDATE_TIME = #{LAST_UPDATE_TIME}
			where 
				ID = #{ID}
	</update>
	
	<!-- 通过kpicode修改 -->
	<update id="updateByKpicode" parameterType="pd">
		update BD_KPI_MODEL_LINE
			set 
				<!-- KPI_CODE = #{KPI_CODE}, -->
				KPI_NAME = #{KPI_NAME},
				KPI_TYPE = #{KPI_TYPE},
				KPI_UNIT = #{KPI_UNIT},
				ENABLE = #{ENABLE},
				LAST_UPDATE_USER = #{LAST_UPDATE_USER},
				LAST_UPDATE_TIME = #{LAST_UPDATE_TIME}
			where 
				KPI_CODE = #{KPI_CODE}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
			<include refid="kpiModelLineColumns"/>
		from 
			BD_KPI_MODEL_LINE
		where 
			ID = #{ID}
	</select>
	
	<!-- 通过code获取数据 -->
	<select id="findByCode" parameterType="pd" resultType="pd">
		select 
			<include refid="kpiModelLineColumns"/>
		from 
			BD_KPI_MODEL_LINE
		where 
			KPI_CODE = #{KPI_CODE}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
			<include refid="kpiModelLineColumns"/>
		from 
			BD_KPI_MODEL_LINE a
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll"  parameterType="pd" resultType="pd">
		select
			<include refid="kpiModelLineColumns"/>
		from 
			BD_KPI_MODEL_LINE a
	</select>
	
	<!-- 根据kpi模板查询列表 -->
	<select id="listAllByModelId" parameterType="String" resultType="pd">
		select
			ID, MODEL_ID, KPI_CODE,	
			KPI_NAME, KPI_TYPE, KPI_UNIT, STATUS, ENABLE,
			CREATE_USER, CREATE_TIME, 
			LAST_UPDATE_USER,
			DATE_FORMAT(LAST_UPDATE_TIME, '%Y-%m-%d %T') LAST_UPDATE_TIME,
			PREPARATION1,
			PREPARATION2,
			PREPARATION3,
			PREPARATION4,
			PREPARATION5,
			PREPARATION6,
			(select NAME from sys_user where USERNAME = a.last_update_user) LAST_UPDATE_EMP_NAME
		from 
			BD_KPI_MODEL_LINE a
		where   
			a.MODEL_ID = #{ID}
	</select>
	
		<!-- 根据kpi模板查询列表 -->
	<select id="listAllModel" parameterType="pd" resultType="pd">
		select
			<include refid="kpiModelLineColumns"/>
		from 
			BD_KPI_MODEL_LINE a
		where   
			a.MODEL_ID = #{ID}
	</select>
	
	<!-- 列表 -->
	<select id="listAllSubByModelId"  parameterType="String" resultType="pd">
		select
			b.ID,
			b.KPI_NAME
		from 
			BD_KPI_MODEL_LINE a left join BD_KPI_FILES b
		on a.KPI_CODE = b.KPI_CODE		
		where a.MODEL_ID = #{MODEL_ID}
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from BD_KPI_MODEL_LINE
		where 
			ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	<!-- 批量删除 -->
	<delete id="deleteAllByModelId" parameterType="pd">
		delete from BD_KPI_MODEL_LINE
		where 
			MODEL_ID = #{MODEL_ID}
	</delete>
	
	<!-- 根据kpicode删除 -->
	<delete id="deleteByKpicode" parameterType="pd">
		delete from BD_KPI_MODEL_LINE
		where 
			KPI_CODE = #{KPI_CODE}
	</delete>
	
	<resultMap type="KpiModelLine" id="KpiModelLineMap">
		<id column="ID" property="ID"/>
		<result column="MODEL_ID" property="MODEL_ID"/>
		<result column="KPI_CODE" property="KPI_CODE"/>
		<result column="KPI_NAME" property="KPI_NAME"/>
		<result column="KPI_TYPE" property="KPI_TYPE"/>
		<result column="KPI_UNIT" property="KPI_UNIT"/>
		<result column="STATUS" property="STATUS"/>
		<result column="ENABLE" property="ENABLE"/>
	</resultMap>
	
	<!-- 批量保存kpi模板中的kpi信息 -->
	<insert id="batchSaveKpiModelLine" parameterType="pd">
		insert into bd_kpi_model_line(
			MODEL_ID, KPI_CODE, KPI_NAME, KPI_type, kpi_unit, status, enable, create_user, create_time, preparation2
		)values
		<foreach collection="list" item="modelLine" separator=",">
			(
				#{modelLine.modelId}, #{modelLine.kpiCode}, #{modelLine.kpiName}, #{modelLine.kpiType}, 
				#{modelLine.kpiUnit}, #{modelLine.status}, #{modelLine.createUser}, now(), #{modelLine.preparation2}
			)
		</foreach>
	</insert>
	
	<!-- 批量删除由考核模板导入的数据 -->
	<delete id="deleteModelLineByImportModel" parameterType="pd">
		delete from bd_kpi_model_line
		where 
			KPI_CODE like concat(#{importCode} ,'%')
			<if test="kpiModelId != null and kpiModelId != ''">
				or MODEL_ID = #{kpiModelId}
			</if>
	</delete>
</mapper>